import numpy as np
import scipy.signal as sig

def filter_bands(filterLength : int, numChannels : int, inputSignal : np.array):
    """
    args :
        - filterLength : [int]
            Digital filter length, in samples
        - numChannels : [int]
            Number of frequency channels in which the signal is to be separated
        - inputSignal : [(1, N) np.array]
            Input signal 
    returns :
        - outputSignals : [numChannels, N) np.array]
            Output signal"""







    return (outputSignal)